绘图功能：
1.默认模式：

模型生成背景图，然后不断生成图像，在背景图上贴图（贴图存name、pos等信息便于模型理解和操作（这里可以设计提示词））

2.快速模式：

（1）先通过语音生成图像（可以生成多个返回供用户选择）

（2）图像悬浮，可以通过语音调整样式、形状、大小、位置

（3）确定则放置，取消则撤回

3.自由绘制模式：

选笔（类型、颜色、粗细）、落笔位置、运行轨迹（直线、曲线（含角度））、提笔位置